# 旸谷智能科技官网搭建指南

根据《旸谷智能科技官网项目说明》整理的完整搭建教程。

---

## 一、项目概述

- **公司名称**：杭州旸谷智能科技有限公司
- **Slogan**：光联万物、智创未来
- **所属行业**：研究和试验发展
- **技术栈**：Hugo 静态网站生成器 + Ananke 主题

---

## 二、前置要求

1. **Hugo Extended**（扩展版本）— 用于自定义 CSS 和资源处理
   - 下载：https://gohugo.io/installation/
   - 验证安装：`hugo version`（需显示 `extended`）

2. **Git** — 用于版本管理和部署

---

## 三、本地开发

### 1. 进入项目目录

```powershell
cd c:\Users\spiry\yanggu-tech
```

### 2. 启动本地服务器

```powershell
hugo server
```

### 3. 访问网站

在浏览器中打开：**http://localhost:1313/**

---

## 四、网站结构说明

根据项目说明，官网应包含以下内容：

| 模块 | 说明 |
|------|------|
| **核心产品与服务** | 端侧（智能传感器）、边侧（边缘计算）、云侧（云平台服务） |
| **合作伙伴** | 合作伙伴介绍 |
| **行业解决方案** | 六大行业解决方案 |
| **应用案例** | 三个典型应用案例 |
| **联系方式** | 公司地址、电话、邮箱、在线留言表单 |
| **社交媒体** | 社交媒体链接 |
| **办公时间** | 办公时间说明 |

### 配色方案（项目说明要求）

- **主色**：科技蓝
- **辅助色**：天空蓝
- **强调色**：橙色
- **背景色**：深色

---

## 五、配置修改指南

### 修改公司基本信息

编辑 `hugo.toml` 或 `config/_default/hugo.toml`：

```toml
baseURL = 'https://yanggu-tech.com/'  # 修改为你的域名
languageCode = 'zh-cn'
title = '旸谷智能科技'
```

### 修改页面内容

- 所有页面内容在 `content/` 目录下的 `.md` 文件中
- 直接编辑对应的 Markdown 文件即可

### 添加新页面

在 `content/` 目录下创建新的 `.md` 文件，例如：

```markdown
---
title: "新页面标题"
description: "页面描述"
---
页面正文内容...
```

### 修改样式（配色）

在 `assets/ananke/css/` 下创建 `custom.css`，定义颜色变量：

```css
:root {
  --color-primary: #2563eb;      /* 科技蓝 */
  --color-secondary: #0ea5e9;    /* 天空蓝 */
  --color-accent: #f97316;       /* 橙色 */
}
```

并在 `config/_default/params.toml` 中注册：

```toml
[params]
custom_css = ["custom.css"]
```

---

## 六、构建与部署

### 构建静态网站

```powershell
hugo
```

生成的文件在 `public/` 目录。

### 部署方式

#### 方法一：Netlify 自动部署（推荐）

1. 在 GitHub 上创建仓库，将项目推送上去
2. 在仓库根目录创建 `netlify.toml`：

```toml
[build]
  command = "hugo"
  publish = "public"
```

3. 在 Netlify 网站连接 GitHub 仓库，自动部署

#### 方法二：手动部署

将 `public/` 目录内容上传到你的 Web 服务器或静态托管服务。

---

## 七、后续优化建议

1. **添加图片资源**：在 `static/images/` 下添加公司 Logo、产品图、团队照片
2. **购买域名**：如 yanggu-tech.com，并配置 DNS
3. **在线留言**：可集成 Formspree 或类似服务
4. **SEO**：为每个页面添加 meta 描述，提交到搜索引擎
5. **评论功能**：如需博客，可集成 Commento

---

## 八、常见问题

### Hugo 命令找不到？

确保 Hugo 已加入系统 PATH，或使用完整路径运行。

### 主题不显示？

检查 `hugo.toml` 中是否配置了 `theme = "ananke"`，以及 `themes/ananke` 子模块是否已拉取：

```powershell
git submodule update --init --recursive
```

### 中文乱码？

确保 Markdown 文件保存为 UTF-8 编码，并在 `hugo.toml` 中设置 `languageCode = "zh-cn"`。

---

## 九、技术支持

- Hugo 官方文档：https://gohugo.io/documentation/
- Ananke 主题文档：https://github.com/theNewDynamic/gohugo-theme-ananke
